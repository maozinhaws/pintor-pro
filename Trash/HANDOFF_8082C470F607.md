# Handoff Sessão MVP — 8082C470F607

> Cole este hash na nova sessão Overclock dentro de `MVP/` para retomar contexto:
> **`8082C470F607`**
>
> Prompt sugerido na nova sessão:
> `Leia HANDOFF_8082C470F607.md e retome o trabalho do MVP a partir desse ponto.`

---

## Contexto

**Projeto:** Pintor Plus — PWA orçamento para pintores (HTML/CSS/JS vanilla + Service Worker).
**Origem:** `D:\Documentos\Projetos Apps\Orçamento_Pintor_Plus\APK_backup_2026-03-27\FINAL\`
**Pasta MVP:** subpasta `MVP/` da origem (cwd da nova sessão).
**Data handoff:** 2026-05-01
**Usuário:** wagner.maniatec@gmail.com

## Decisões Travadas

1. **Escopo MVP = subset MÍNIMO.** SEM Google Drive, SEM Google Calendar, SEM Google Contacts, SEM html2pdf, SEM OAuth/GSI, SEM Web Crypto.
2. **Manter:** criar orçamento básico, listar clientes locais via LocalStorage, gerar resumo na tela. Funcional offline puro.
3. **Stack final MVP:** HTML5 + CSS3 + Vanilla JS + Service Worker (cache estático) + manifest PWA.

## Arquivos Copiados para MVP/ (24)

`app.html`, `index.html`, `sw.js`, `site.webmanifest`, `manifest.json`, `vercel.json`, `_headers`, `_redirects`, `config.json`, `package.json`, `package-lock.json`, `privacy-policy.html`, `DOCUMENTACAO_TECNICA.md`, `README.md`, `favicon.ico`, `favicon.svg`, `favicon-16x16.png`, `favicon-32x32.png`, `favicon-96x96.png`, `apple-touch-icon.png`, `android-chrome-192x192.png`, `android-chrome-512x512.png`, `web-app-manifest-192x192.png`, `web-app-manifest-512x512.png`.

**NÃO copiados (intencional):** `__pycache__`, `*.zip`, `debug*`, `demo*`, `extract*`, `temp.js`, `experiments/`, `migration/`, `pintor-plus-flash/`, `pintor-plus-main/`, `Native_App_Project/`, `skills/`, scripts Python (`*.py`), `STATE.md`, `pendencias.md`, `flash*`.

## Swarm Ativo (sessão anterior)

4 panes rodando na sessão antiga, cwd = `MVP/`:

| paneId | Model | Papel |
|--------|-------|-------|
| `Ph3LGEOS` | claude-opus-4-7 | Refator `app.html` — remover Drive/PDF/OAuth/Calendar/Contacts/WebCrypto, manter UI orçamento + LocalStorage |
| `S2QVC9R_` | claude-sonnet-4-6 | Simplificar `sw.js` (cache estático puro) + revisar `site.webmanifest` + `manifest.json` |
| `dZAY3SLB` | claude-sonnet-4-6 | Simplificar `index.html` landing + reescrever `README.md` + criar `MVP_NOTES.md` |
| `8zOwhLTi` | claude-sonnet-4-6 | Auditoria segurança: `vercel.json`, `_headers`, `_redirects`, `privacy-policy.html` (remover menções Drive/Calendar) |

> Os panes foram instruídos a propor plano antes de editar. Verifique status de cada um com `pane_list` na sessão antiga ou recrie/continue na nova sessão.

## Preferências Confirmadas pelo Usuário

- Idioma: pt-BR.
- Modo caveman ativo (`/caveman full`) — respostas terse, fragmentos OK.
- Confirma ações destrutivas antes de executar.
- Workflow Q&A enumerado (`1a/1b`, `2 sim/não`).

## Próximos Passos Sugeridos

1. Verificar progresso dos 4 panes (se sessão antiga ainda viva).
2. Se sessão antiga morta: respawnar swarm OU continuar manualmente o refator `app.html`.
3. Após refator: testar `app.html` no browser (criar orçamento, salvar cliente, recarregar).
4. Validar Service Worker registrando + manifest válido (Lighthouse PWA).
5. Deploy teste Vercel.

## Comandos Úteis

```powershell
# Listar panes ativos do workspace antigo (se aplicável)
# Use mcp__overclock__pane_list

# Ver arquivos MVP
Get-ChildItem .

# Servir local rápido para testar PWA
npx http-server . -p 8080
```
